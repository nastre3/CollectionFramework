public class DynamicStringArray extends CustomArrayList<String> { // <> передается в DynamicArray, T - DynamicStringArray.
    // Если в DynamicStringArray - Integer, в DynamicArray попадет тоже Integer
}
